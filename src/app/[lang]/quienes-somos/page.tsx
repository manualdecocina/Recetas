import { InstitutionalPage } from '@/components/InstitutionalPage'
export default async function Page({ params }: { params: Promise<{ lang: string }> }) { const { lang } = await params; return <InstitutionalPage lang={lang} eyebrow="Manual de Cocina" title="Quiénes somos" intro="Un proyecto editorial gastronómico para cocinar mejor, con recetas claras y herramientas útiles."><p>Manual de Cocina es un proyecto editorial gastronómico digital. Reunimos recetas, categorías, ingredientes y recursos de cocina en una experiencia pensada para descubrir qué cocinar y hacerlo con confianza.</p><p>El proyecto no es un restaurante, no presta servicios profesionales y no mantiene una tienda de productos.</p></InstitutionalPage> }
